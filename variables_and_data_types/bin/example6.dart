void main(List<String> args) {
  const int yourAge = 20;
  const myAge = 20;
  print(yourAge);
  print(myAge);
}
///type inference was discussed as dart has the ability to infer
///your data types automatically (where it can)
